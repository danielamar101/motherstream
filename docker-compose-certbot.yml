services: 
  certbot:
    container_name: certbot
    image: certbot/certbot:latest
    network_mode: host
    dns:
      - 8.8.8.8
      - 1.1.1.1
    volumes:
      - ./certbot/www/:/var/www/certbot/:rw
      - ./certs/:/etc/letsencrypt/:rw
      - ./logs/:/var/log/letsencrypt/:rw
    command: certonly -v --webroot --webroot-path /var/www/certbot -d staging.motherstream.live --email itsalways12@gmail.com --agree-tos --force-renewal
    # container_name: certbot
    # image: certbot/certbot:latest
    # volumes:
    #   - ./certbot/www/:/var/www/certbot/:rw
    #   - ./certs/:/etc/letsencrypt/:rw
    # command: certonly -v --manual --preferred-challenges dns -d always12.live --email itsalways12@gmail.com --agree-tos --no-eff-email -it


